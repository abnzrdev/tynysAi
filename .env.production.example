# Production Environment Configuration for Tynys Application
# Copy this file to .env.production and fill in with actual production values
# NEVER commit .env.production to git - it contains sensitive credentials

# The public URL where your app is hosted (production domain)
NEXTAUTH_URL=https://your-production-domain.com

# Production PostgreSQL Database Connection
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
# Note: Host should be the database container/server name or IP
DB_URL=postgresql://admin:your_secure_password@tynys-postgres:5432/tynysdb

# Alternative database URL (if using DATABASE_URL instead)
DATABASE_URL=postgresql://admin:your_secure_password@tynys-postgres:5432/tynysdb

# NextAuth encryption secret for session tokens
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_generated_secret_here

# Secret for IoT device API authentication
# This is used in the Authorization header for CSV data ingestion
IOT_DEVICE_SECRET=your_secure_iot_device_secret
